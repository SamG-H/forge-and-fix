<div class="container has-text-centered">
  <h1 class="is-size-1">Projects</h1>
  <div>
    <% @projects.each do |p| %>
      <p><%= p.title %>: <%= p.category %></p>
      <% end %>
  </div>
<%= link_to "Create New Project", new_project_path, class:"button is-success" %>
</div>
